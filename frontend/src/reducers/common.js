const initialState = {
    isMobile: null
}

export default (state = initialState, action) => {
    // console.log("REDUCER:", action);
    switch (action.type) {
        default:
            return state
    }

}